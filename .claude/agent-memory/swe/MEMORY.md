- [per-location alpha_1 wiring](project_per_location_alpha1_wiring.md) — alpha_1 made per-location length-nL end-to-end (v0.51.0); dual-mode gotchas + the get_location_config sibling the plan missed; installed-wheel vs stale src
- [hedgehog-run infra](project_hedgehog_run_infra.md) — hedgehog VM run mechanics + two vm/ helper traps (broken ETH template, pull_results.sh does not compress)
- [dugong VM](reference_dugong_vm.md) — 2nd run host (176c/1.5TiB, Ubuntu 24.04, persistent IP 52.183.44.192); libexpat-wrapper trap (not GLIBCXX); skill .claude/skills/dugong-run, doc vm/DUGONG.md, setup claude/dugong_setup/
- [Coiled w/ dugong client validated](project_coiled_dugong_client_validated.md) — dugong-as-Dask-client fixes the 2026.3.0↔2026.6.0 skew that stalled laptop (sims_done=0); GO for prod Coiled fits; +OpenSSL-3.3.0 LD_PRELOAD trap (libcrypto/libssl, not just libexpat)
- [cairo_pdf glyph trap](cairo_pdf_glyph_trap.md) — capabilities("cairo") lies on this box; probe a real cairo_pdf device + fall back to ASCII labels for PDF Unicode glyphs
- [optimize_subset levers](project_optimize_subset_levers.md) — optimize_ensemble_subset() A/B/C speedups; the ci=(i-1)*n_times+j scatter contract + byrow=TRUE parity trap; PSOCK-not-FORK; install-before-parallel-test
- [PSOCK blocking-gather worker-death deadlock](psock_blocking_gather_worker_death_deadlock.md) — parLapply/pblapply(cl=) unserialize() hangs master FOREVER on Linux when a worker PROCESS crashes; fix=.mosaic_cluster_lapply_robust socketSelect-timeout gather (v0.49.1); was misdiagnosed as B2/CFR_target-specific
- [single-location psi_jt nameless](project_single_location_psi_jt_nameless.md) — get_location_config psi_jt has NO colnames + date_start/stop are strings; align psi swaps positionally via seq.Date(), LOCF a bounded tail gap
- [2015-window runtime blast](project_2015_window_runtime_blast.md) — env-var contract robust at runtime (date_start never read from env at run); engine/per-worker memory a non-issue; REAL OOM cliff = calc_model_ensemble dense 4-D arrays (~40GB at 1000x10 / 2015 width); #82 boundary NOT amplified; plot 3-month breaks unreadable over 11yr
- [scoring-window burn-in](project_scoring_window_burnin.md) — per-channel scored window (control$likelihood, default no-op/bit-identical); SLICE not weights_time=0 (shape terms ignore weights); hot-loop precompute + sliced date_start; local/Dask scoring divergence (engine py LL has no weights_obs_*, Dask NaNs deaths prefix)
- [Dask worker likelihood v0.16.0 parity](project_dask_worker_likelihood_v016_parity.md) — weights_obs port parity OK to 1e-11; loc_idx peak-sourcing trap (read peaks from model.params not config); WIS np.sum NaN engine bug (read-only); dual-mode alpha; stale local repo vs installed wheel; conda-not-venv reticulate
- [Dask/local dtype parity (v0.48.2)](project_dask_local_dtype_parity_v0482.md) — engine as_ndarray keeps numpy float64 but casts lists to uint32/float32 -> worker must ship _AS_NDARRAY_FIELDS as LISTS (1-2% LL divergence else); weight matrices must NOT leak into per-sim JSON (toJSON NA->"NA" crash); now bit-identical engine + 2e-15 scorer
