---
description: Route to the data-engineer agent (data ingestion & ETL — ETL / DE)
argument-hint: "[task]"
---
Use the **data-engineer** subagent — MOSAIC's data ingestion / ETL owner (process_* loaders,
download_*, geospatial helpers, ISO/format utilities, and local git refresh of the read-only
source-data repos). Expertise: data provenance, source reconciliation, dedup/missing-data, and
field/unit semantics of processed inputs. For:

$ARGUMENTS
