---
description: Route to the statistician agent (Bayesian/math core — STAT / STATS)
argument-hint: "[task]"
---
Use the **statistician** subagent — the MOSAIC Bayesian statistician/mathematician (likelihood,
Gibbs-posterior weighting, ESS/agreement/CVW convergence, weighted ensemble & posterior quantiles,
WIS, R²/bias, distribution fitting) — for:

$ARGUMENTS
