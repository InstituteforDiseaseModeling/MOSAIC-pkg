% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_bfrs_posterior.R
\name{calc_bfrs_posterior}
\alias{calc_bfrs_posterior}
\title{Calculate BFRS Posterior via Importance Sampling}
\usage{
calc_bfrs_posterior(
  results,
  param_names,
  n_samples = 10000,
  method = "importance_sampling",
  log_likelihood = TRUE,
  temperature = 1,
  verbose = FALSE
)
}
\arguments{
\item{results}{Data frame containing simulation results with parameters and likelihoods}

\item{param_names}{Character vector of parameter names to include in posterior}

\item{n_samples}{Integer number of posterior samples to generate (default 10000)}

\item{method}{Character string specifying method: "importance_sampling" or "rejection_sampling"}

\item{log_likelihood}{Logical whether likelihood column is in log scale (default TRUE)}

\item{temperature}{Numeric temperature parameter for tempering (default 1.0)}

\item{verbose}{Logical whether to print progress messages}
}
\value{
List containing:
\describe{
\item{samples}{Matrix of posterior samples (n_samples × n_params)}
\item{weights}{Vector of importance weights for original simulations}
\item{log_weights}{Log-scale importance weights}
\item{ess}{Effective sample size}
\item{mean}{Posterior mean estimates}
\item{cov}{Posterior covariance matrix}
\item{quantiles}{Posterior quantiles at standard levels}
\item{log_evidence}{Log marginal likelihood estimate}
}
}
\description{
Creates a posterior approximation from BFRS simulation results using
importance sampling with likelihood weights.
}
\examples{
\dontrun{
# After running BFRS simulations
bfrs_posterior <- calc_bfrs_posterior(
    results = results,
    param_names = c("tau_i_ETH", "mobility_gamma_ETH", "mu_j_ETH"),
    n_samples = 10000,
    verbose = TRUE
)
}
}
