% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_log_likelihood_distributions.R
\name{calc_log_likelihood_binomial}
\alias{calc_log_likelihood_binomial}
\title{Calculate log-likelihood for Binomial-distributed data}
\usage{
calc_log_likelihood_binomial(
  observed,
  estimated,
  trials,
  weights = NULL,
  verbose = TRUE
)
}
\arguments{
\item{observed}{Integer vector of observed successes (e.g., number of positives).}

\item{estimated}{Numeric vector of expected probabilities of success in (0, 1).}

\item{trials}{Integer vector of total trials (same length as \code{observed}).}

\item{weights}{Optional numeric vector of non-negative weights, same length as \code{observed}.
Default is \code{NULL}, which sets all weights to 1.}

\item{verbose}{Logical; if \code{TRUE}, prints total log-likelihood and checks for data consistency.}
}
\value{
A scalar log-likelihood (numeric).
}
\description{
Computes the total log-likelihood for integer counts of successes under the Binomial distribution,
optionally weighting each observation via \code{weights}.
}
\details{
The Binomial distribution models the number of successes in fixed trials:
\deqn{
Y \\sim \\text{Binomial}(n, p).
}
Weighted by \code{weights[i]} for each observation \code{i}.
}
\examples{
calc_log_likelihood_binomial(c(3, 4, 2), c(0.3, 0.5, 0.25), c(10, 10, 8))
}
