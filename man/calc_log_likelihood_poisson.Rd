% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_log_likelihood_distributions.R
\name{calc_log_likelihood_poisson}
\alias{calc_log_likelihood_poisson}
\title{Calculate log-likelihood for Poisson-distributed count data}
\usage{
calc_log_likelihood_poisson(
  observed,
  estimated,
  weights = NULL,
  zero_buffer = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{observed}{Integer vector of observed non-negative counts (e.g., cases, deaths).}

\item{estimated}{Numeric vector of expected values from the model (same length as \code{observed}).}

\item{weights}{Optional numeric vector of non-negative weights, same length as \code{observed}.
Default is \code{NULL}, which sets all weights to 1.}

\item{zero_buffer}{Logical; if \code{TRUE} (default), rounds observed values to integers and
adds small buffer to avoid zero estimates. If \code{FALSE}, enforces
strict integer requirements.}

\item{verbose}{Logical; if \code{TRUE}, prints diagnostics and total log-likelihood.}
}
\value{
A scalar representing the total log-likelihood (numeric).
}
\description{
Computes the total log-likelihood for integer count data under the Poisson distribution.
Each observation can be weighted via \code{weights}, defaulting to equal weights.
}
\details{
The Poisson distribution assumes that the variance equals the mean, \\( \\text{Var}(Y) = \\mu \\).
Weighted log-likelihood is summed across observations. If the variance/mean ratio is > 1.5,
a warning about possible overdispersion is issued.
}
\examples{
calc_log_likelihood_poisson(c(2, 3, 4), c(2.2, 2.9, 4.1))

}
