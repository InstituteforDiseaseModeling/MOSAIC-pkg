% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_model_convergence.R
\name{calc_model_akaike_weights}
\alias{calc_model_akaike_weights}
\title{Truncated Akaike weights with optional temperature scaling}
\usage{
calc_model_akaike_weights(delta, delta_max = 6, temperature = 1)
}
\arguments{
\item{delta}{Numeric vector of AIC differences \eqn{\Delta_i}. Derived from \code{\link[=calc_model_aic_delta]{calc_model_aic_delta()}}.}

\item{delta_max}{Numeric Δ cutoff for truncation (default \code{6}). Weights with
\eqn{\Delta_i > \texttt{delta_max}} are set to zero; set to \code{Inf} to disable.}

\item{temperature}{Positive scalar temperature \eqn{\tau}; \code{1} reproduces
standard Akaike weights (default). \eqn{\tau > 1} flattens weights (raises ESS),
\eqn{\tau < 1} sharpens (more concentration on the top draw).}
}
\value{
A list with:
\itemize{
\item \code{w}        — raw (possibly truncated) Akaike weights (after temperature).
\item \code{w_tilde}  — normalized weights \eqn{\tilde w}.
\item \code{retained} — logical vector; TRUE if \eqn{\Delta_i \le \texttt{delta_max}}.
\item \code{B_idx}    — integer indices of retained draws.
\item \code{delta_max}— the cutoff used.
\item \code{temperature} — the temperature used.
}
}
\description{
Converts \eqn{\Delta} values into Akaike weights
\deqn{w_i(\tau) \propto \exp\!\left(-\tfrac{1}{2}\Delta_i / \tau\right)}
and normalized weights \eqn{\tilde w_i = w_i/\sum_j w_j}, with optional
truncation at \code{delta_max}. The temperature \eqn{\tau} flattens
(\eqn{\tau > 1}) or sharpens (\eqn{\tau < 1}) the weight distribution.
}
\section{Choosing \code{temperature}}{

\itemize{
\item \strong{Default:} \code{temperature = 1}.
\item \strong{Mild flattening:} \code{1.5–2}. Use when one or two draws dominate
and you want smoother posterior plots and higher ESS.
\item \strong{Strong flattening:} \code{3–5}. Use sparingly; weights become
nearly uniform and discrimination drops. A practical target is to pick
\code{temperature} so that \code{max(normalized weight) \u2264 0.5}.
}
}

\examples{
set.seed(1)
ll <- 1000 + rnorm(200, sd = 3)
d  <- calc_model_aic_delta(ll)
# Standard (tau = 1)
aw1 <- calc_model_akaike_weights(d, delta_max = 20)
# Mild flattening
aw2 <- calc_model_akaike_weights(d, delta_max = 20, temperature = 2)
c(max_w1 = max(aw1$w / sum(aw1$w)), max_w2 = max(aw2$w / sum(aw2$w)))

}
\seealso{
\code{\link[=calc_model_aic_delta]{calc_model_aic_delta()}}, \code{\link[=calc_model_convergence]{calc_model_convergence()}}

Other calibration-metrics: 
\code{\link{calc_model_agreement_index}()},
\code{\link{calc_model_aic_delta}()},
\code{\link{calc_model_convergence}()},
\code{\link{calc_model_cvw}()},
\code{\link{calc_model_ess}()},
\code{\link{calc_model_max_weight}()},
\code{\link{plot_model_convergence}()}
}
\concept{calibration-metrics}
