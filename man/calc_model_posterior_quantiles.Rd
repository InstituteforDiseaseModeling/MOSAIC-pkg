% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_model_posterior_quantiles.R
\name{calc_model_posterior_quantiles}
\alias{calc_model_posterior_quantiles}
\title{Calculate Prior and Posterior Quantiles with KL Divergence}
\usage{
calc_model_posterior_quantiles(
  results,
  probs = c(0.025, 0.25, 0.5, 0.75, 0.975),
  output_dir = "./results",
  verbose = TRUE
)
}
\arguments{
\item{results}{Data frame with calibration results containing parameter columns
and index columns (is_finite, is_retained, is_best_subset)}

\item{probs}{Numeric vector of quantile probabilities (default: c(0.025, 0.25, 0.5, 0.75, 0.975))}

\item{output_dir}{Directory path to save results (default: "./results")}

\item{verbose}{Logical; print progress messages (default: TRUE)}
}
\value{
Data frame with both prior and posterior quantiles plus KL divergence
}
\description{
Calculates quantiles for both prior (all simulations) and posterior (weighted best subset)
distributions, with KL divergence between them using KDE.
}
\details{
The function:
\enumerate{
\item Calculates unweighted quantiles from ALL finite simulations (prior)
\item Calculates weighted quantiles from best subset (posterior)
\item Computes KL divergence using KDE on empirical distributions
\item Returns both prior and posterior rows with a 'type' identifier
\item KL divergence appears only in posterior rows
}
}
\seealso{
\code{\link{weighted_quantiles}}, \code{\link{calc_model_ess}}
}
