% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate_psi_predictions.R
\name{calibrate_psi_predictions}
\alias{calibrate_psi_predictions}
\title{Per-country affine bias-calibration of suitability (psi) predictions}
\usage{
calibrate_psi_predictions(
  pred_df,
  obs_df,
  fit_date_stop,
  pred_col = "pred_smooth",
  obs_col = "transmission_intensity",
  out_col = "pred_calibrated",
  min_train = 8L
)
}
\arguments{
\item{pred_df}{Data frame of predictions with at least \code{iso_code},
\code{date}, and \code{pred_col}.}

\item{obs_df}{Data frame of observed targets with \code{iso_code},
\code{date}, and \code{obs_col}.}

\item{fit_date_stop}{Date (or coercible); calibration is fit on
\code{date <= fit_date_stop}.}

\item{pred_col}{Prediction column to calibrate (default \code{"pred_smooth"}).}

\item{obs_col}{Observed-target column (default \code{"transmission_intensity"}).}

\item{out_col}{Name of the calibrated output column (default
\code{"pred_calibrated"}).}

\item{min_train}{Minimum training points per country to fit; otherwise identity
(default 8).}
}
\value{
\code{pred_df} with an added \code{out_col} (calibrated, clipped to
\eqn{[0,1]}).
}
\description{
Fits a per-country linear recalibration \eqn{obs \approx a_i \cdot pred + b_i}
on the \strong{training window only} and applies it to all predictions, clipped
to \eqn{[0, 1]}. Addresses the systematic negative bias (right shape, wrong
scale) of the suitability LSTM without altering its training.
}
\details{
For each \code{iso_code}, the affine coefficients are estimated by OLS on the
merged (prediction, observed) pairs with \code{date <= fit_date_stop} — i.e.
the training period only, so the calibration is leakage-clean with respect to
the out-of-sample window. Countries with fewer than \code{min_train} usable
training points (or zero predictor variance) fall back to the identity
transform with a warning.
}
\seealso{
\code{\link{est_suitability}}
}
