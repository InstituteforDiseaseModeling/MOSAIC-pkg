% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_parameters.R
\name{create_sampling_args}
\alias{create_sampling_args}
\title{Create Sampling Arguments for Common Patterns}
\usage{
create_sampling_args(
  pattern = "all",
  seed,
  custom = list(),
  PATHS = NULL,
  priors = NULL,
  config = NULL
)
}
\arguments{
\item{pattern}{Character string specifying the pattern. Options:
\itemize{
\item "all": Sample all parameters (default)
\item "none": Don't sample any parameters
\item "disease_only": Sample only disease progression and immunity parameters
\item "transmission_only": Sample only transmission parameters
\item "mobility_only": Sample only mobility parameters
\item "spatial_only": Sample only spatial parameters
\item "initial_conditions_only": Sample only initial condition proportions
}}

\item{seed}{Random seed for sampling}

\item{custom}{Named list of custom overrides for specific parameters}

\item{PATHS}{Optional PATHS object}

\item{priors}{Optional priors object}

\item{config}{Optional config object}
}
\value{
Named list of arguments suitable for do.call(sample_parameters, ...)
}
\description{
Helper function to create argument lists for common sampling scenarios,
making it easier to work with the many parameters.
}
\examples{
\dontrun{
# Sample only disease parameters
args <- create_sampling_args("disease_only", seed = 123)
config <- do.call(sample_parameters, args)

# Sample all except mobility
args <- create_sampling_args("all", seed = 123,
  custom = list(sample_mobility_omega = FALSE,
                sample_mobility_gamma = FALSE))
config <- do.call(sample_parameters, args)
}
}
