% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/downscale_weekly_values.R
\name{downscale_weekly_values}
\alias{downscale_weekly_values}
\title{Downscale weekly values to a daily time series}
\usage{
downscale_weekly_values(date_start, value, integer = TRUE)
}
\arguments{
\item{date_start}{A vector of Dates (class "Date") representing the start of each week.
Must fall on Mondays.}

\item{value}{A numeric vector of weekly totals (e.g., cases or rainfall).}

\item{integer}{Logical; if TRUE, treat values as counts and distribute remainder symmetrically.}
}
\value{
A data frame with columns: \code{date}, \code{value}, one row per day from min to max date.
}
\description{
This function takes a weekly time series (start dates and values) and returns a complete
daily time series with values distributed across the 7 days of each week. If \code{integer = TRUE},
remainders are centered in the week. Missing weeks or days are filled with NA to ensure completeness.
}
\examples{

downscale_weekly_values(as.Date(c("2023-01-02", "2023-01-16")), c(10, 3), integer = TRUE)

}
