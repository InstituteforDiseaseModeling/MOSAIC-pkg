% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/est_seasonal_dynamics.R,
%   R/est_seasonal_dynamics_2.R
\name{est_seasonal_dynamics}
\alias{est_seasonal_dynamics}
\title{Estimate Seasonal Dynamics for Cholera and Precipitation Using Daily Fourier Series}
\usage{
est_seasonal_dynamics(
  PATHS,
  date_start,
  date_stop,
  min_obs = 30,
  clustering_method,
  k
)

est_seasonal_dynamics(
  PATHS,
  date_start,
  date_stop,
  min_obs = 30,
  clustering_method,
  k
)
}
\arguments{
\item{PATHS}{A list containing paths where raw and processed data are stored. See \code{get_paths()}.}

\item{date_start}{A date in YYYY-MM-DD format indicating the start date of the precipitation data.}

\item{date_stop}{A date in YYYY-MM-DD format indicating the stop date of the precipitation data.}

\item{min_obs}{The minimum number of observations required to fit the Fourier series to cholera case data.}

\item{clustering_method}{The clustering method to use (e.g., "kmeans", "ward.D2", etc.).}

\item{k}{Number of clusters for grouping countries by seasonality.}

\item{exclude_iso_codes}{Optional character vector of ISO codes to exclude from clustering and neighbor matching.}
}
\value{
Saves daily fitted values and parameter estimates to CSV.

Saves daily fitted values and parameter estimates to CSV.
}
\description{
This function retrieves historical precipitation data, processes cholera case data,
and fits seasonal dynamics models using a double Fourier series at the daily scale (p = 365).
Weekly data is expanded by assigning each weekly value to every day in that week.

This function retrieves historical precipitation data, processes cholera case data,
and fits seasonal dynamics models using a double Fourier series at the daily scale (p = 365).
Weekly data is expanded by assigning each weekly value to every day in that week.
}
