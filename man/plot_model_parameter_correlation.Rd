% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_model_parameter_correlation.R
\name{plot_model_parameter_correlation}
\alias{plot_model_parameter_correlation}
\title{Plot Posterior Parameter Correlation Heatmap}
\usage{
plot_model_parameter_correlation(
  results_file,
  priors_file = NULL,
  output_dir = ".",
  cor_threshold = 0.3,
  max_params = 25,
  subset_col = "is_best_subset",
  verbose = TRUE
)
}
\arguments{
\item{results_file}{Path to samples.parquet file containing calibration results}

\item{priors_file}{Path to priors.json (used for parameter descriptions)}

\item{output_dir}{Directory to write the output figure}

\item{cor_threshold}{Minimum absolute correlation to display as text label.
Default 0.3.}

\item{max_params}{Maximum number of parameters to display. The top
\code{max_params} most-correlated parameters are selected. Default 25.}

\item{subset_col}{Character name of the boolean subset-membership column.
Defaults to \code{"is_best_subset"}. Pass \code{"is_best_subset_opt"} to
read the optimizer-refined subset.}

\item{verbose}{Print progress messages}
}
\value{
Invisible NULL. Writes a PNG file to \code{output_dir}.
}
\description{
Generates a heatmap of pairwise Spearman correlations between parameters in
the posterior (best subset), revealing trade-offs and redundancies. Parameters
are clustered hierarchically and only correlations exceeding a threshold are
labeled.
}
