% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_model_posterior_quantiles.R
\name{plot_model_posterior_quantiles}
\alias{plot_model_posterior_quantiles}
\title{Plot Model Posterior Quantiles}
\usage{
plot_model_posterior_quantiles(
  csv_files,
  output_dir = "./figures",
  plot_types = "both",
  verbose = TRUE
)
}
\arguments{
\item{csv_files}{Character vector of paths to posterior_quantiles.csv files
from calc_model_posterior_quantiles(), estimate_parameters_npe(), or estimate_parameters_lampe()}

\item{output_dir}{Directory to save plots (default: "./figures")}

\item{plot_types}{Character vector specifying which plot types to create
("global", "location", "both"). Default: "both"}

\item{verbose}{Logical, whether to print progress messages (default: TRUE)}
}
\value{
List of ggplot objects (invisibly)
}
\description{
Creates visualizations of posterior parameter quantiles from calibration or NPE results.
Generates both global parameter plots and location-specific parameter plots.
Supports comparison of multiple posterior sources.
}
\details{
The function creates:
\itemize{
\item Caterpillar plots showing median and credible intervals for each estimation type
\item Separate plots for global and location-specific parameters
\item Color-coded visualization by estimation type (automatically detected):
\itemize{
\item Prior samples (dark grey)
\item Calibration posterior (blue)
\item NPE estimates (red/orange)
\item Lampe estimates (bright red)
\item Other types (automatically assigned from color palette)
}
\item Supports comparison of any number of estimation types
\item Dynamically adapts to whatever types are present in the data
\item Expects standardized CSV format with consistent column names
\item Saves plots as PNG files in the output directory
}
}
\examples{
\dontrun{
# Single posterior file
plot_model_posterior_quantiles("posterior_quantiles.csv")

# Compare multiple sources
plot_model_posterior_quantiles(
  csv_files = c("bfrs_quantiles.csv", "npe_quantiles.csv"),
  output_dir = "./comparison_plots"
)
}

}
