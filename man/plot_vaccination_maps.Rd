% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_vaccination_maps.R
\name{plot_vaccination_maps}
\alias{plot_vaccination_maps}
\title{Plot Vaccination Maps}
\usage{
plot_vaccination_maps(PATHS, data_source = "WHO")
}
\arguments{
\item{PATHS}{A named list containing file paths required for input and output operations. It should include:
\describe{
\item{\code{MODEL_INPUT}}{Directory path where the processed vaccination data files are located.}
\item{\code{DOCS_FIGURES}}{Directory path where the generated plots will be saved.}
}}

\item{data_source}{The source of the vaccination data. Must be one of \code{"WHO"}, \code{"GTFCC"}, or \code{"BOTH"}. Default is \code{"WHO"}.}
}
\value{
The function does not return any value. It generates and saves a PNG file containing the vaccination maps and barplots with annotations.
}
\description{
Generates choropleth maps and corresponding bar plots to visualize OCV vaccination data from WHO, GTFCC, or combined sources across African countries.
}
\details{
The function performs the following steps:
\enumerate{
\item \strong{Data Loading and Processing}: Reads the vaccination data from a CSV file, converts the \code{date} column to \code{Date} objects, and extracts the latest cumulative doses distributed and corresponding vaccination proportions for each country.
\item \strong{Map Data Retrieval}: Obtains map data for African countries using the \code{rnaturalearth} package.
\item \strong{Data Merging}: Merges the vaccination data with the map data, identifying Sub-Saharan African countries based on ISO codes provided by the \code{MOSAIC::iso_codes_ssa} dataset.
\item \strong{Color Scale Definition}: Defines distinct color scales for cumulative doses distributed (green gradient) and proportion vaccinated (purple gradient), including appropriate breaks and labels.
\item \strong{Choropleth Map Creation}: Creates two choropleth maps—one for cumulative doses distributed and another for the proportion vaccinated—using \code{ggplot2} and layering map data accordingly.
\item \strong{Barplot Generation}: Constructs barplots for each metric, ordering countries by descending values and applying the corresponding color scales.
\item \strong{Plot Arrangement}: Utilizes the \code{patchwork} and \code{cowplot} packages to arrange the maps and barplots into a structured grid with panel labels "A" and "B" and adds a footnote.
\item \strong{Saving the Plot}: Saves the final combined plot as a PNG file in the specified \code{DOCS_FIGURES} directory.
}
}
\examples{
\dontrun{
# Define the PATHS list with appropriate directories
PATHS <- list(
  MODEL_INPUT = "path/to/model/input",
  DOCS_FIGURES = "path/to/docs/figures"
)

# Call the function to generate and save the vaccination maps
plot_vaccination_maps(PATHS)
plot_vaccination_maps(PATHS, data_source = "GTFCC")
plot_vaccination_maps(PATHS, data_source = "BOTH")
}

}
