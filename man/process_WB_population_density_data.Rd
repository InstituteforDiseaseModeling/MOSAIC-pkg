% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_WB_population_density_data.R
\name{process_WB_population_density_data}
\alias{process_WB_population_density_data}
\title{Process World Bank Population Density Data}
\usage{
process_WB_population_density_data(PATHS)
}
\arguments{
\item{PATHS}{List. Project paths object with components \code{DATA_RAW} and \code{DATA_PROCESSED}.
\itemize{
\item Expects the raw file at:
\code{file.path(PATHS$DATA_RAW, 'world_bank', 'API_EN.POP.DNST_DS2_en_csv_v2_85433.csv')}
\item Will write output to:
\code{file.path(PATHS$DATA_PROCESSED, 'world_bank', 'pop_density_data_world_bank.csv')}
}}
}
\value{
A data.frame with columns:
\describe{
\item{iso_code}{ISO country code (character)}
\item{year}{Year (integer)}
\item{pop_density}{Population density (numeric, people per sq. km)}
}
Invisibly returns the data.frame after writing the CSV.
}
\description{
Reads the raw World Bank population density CSV (skipping the first 4 metadata rows), reshapes it to long format via a for loop,
writes the processed data as a CSV to disk, and returns it. Uses base R functions only.
}
