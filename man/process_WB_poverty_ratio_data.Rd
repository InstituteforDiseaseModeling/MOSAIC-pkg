% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_WB_poverty_ratio_data.R
\name{process_WB_poverty_ratio_data}
\alias{process_WB_poverty_ratio_data}
\title{Process World Bank Poverty Ratio Data}
\usage{
process_WB_poverty_ratio_data(PATHS)
}
\arguments{
\item{PATHS}{List. Project paths object with components \code{DATA_RAW} and \code{DATA_PROCESSED}.
\itemize{
\item Expects the raw file located in:
\code{file.path(PATHS$DATA_RAW, 'world_bank', '<poverty_ratio_filename>.csv')}
\item Will write output to:
\code{file.path(PATHS$DATA_PROCESSED, 'world_bank', 'poverty_ratio_data_world_bank.csv')}
}}
}
\value{
A data.frame with columns:
\describe{
\item{iso_code}{ISO country code (character)}
\item{year}{Year (integer)}
\item{poverty_ratio}{Poverty ratio (numeric, percent of population below poverty line)}
}
Invisibly returns the data.frame after writing the CSV.
}
\description{
Reads the raw World Bank poverty ratio CSV (skipping the first 4 metadata rows), reshapes it to long format via a for loop,
writes the processed data as a CSV to disk, and returns it. Uses base R functions only.
}
