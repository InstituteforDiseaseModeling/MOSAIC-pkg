% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_obj_to_list.R
\name{read_obj_to_list}
\alias{read_obj_to_list}
\title{Read a Python-Compatible Object File into an R List}
\usage{
read_obj_to_list(file_path)
}
\arguments{
\item{file_path}{A character string specifying the full file path of the \code{.obj} or \code{.obj.gz} file to load.}
}
\value{
A named R list containing the data from the \code{.obj} file.
}
\description{
Reads a Python-compatible \code{.obj} or \code{.obj.gz} file and converts it back into an R list.
}
\details{
This function uses \code{sciris.sc.load()} from Python to read the \code{.obj} file, which is then converted back into
an R list using \code{reticulate::py_to_r()}. If the file is compressed (\code{.obj.gz}), it is automatically decompressed.
The function assumes that the virtual Python environment has already been activated via \code{.onAttach()}.
}
\examples{
\dontrun{
  # Load an uncompressed .obj file
  params <- read_obj_to_list("config.obj")

  # Load a compressed .obj.gz file
  params_gz <- read_obj_to_list("config.obj.gz")

  # Inspect contents
  str(params)
}

}
