% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_parameters.R
\name{sample_parameters}
\alias{sample_parameters}
\title{Sample Parameters from Prior Distributions}
\usage{
sample_parameters(
  PATHS = NULL,
  priors = NULL,
  config = NULL,
  seed,
  sample_alpha_1 = TRUE,
  sample_alpha_2 = TRUE,
  sample_decay_days_long = TRUE,
  sample_decay_days_short = TRUE,
  sample_decay_shape_1 = TRUE,
  sample_decay_shape_2 = TRUE,
  sample_epsilon = TRUE,
  sample_gamma_1 = TRUE,
  sample_gamma_2 = TRUE,
  sample_iota = TRUE,
  sample_kappa = TRUE,
  sample_mobility_gamma = TRUE,
  sample_mobility_omega = TRUE,
  sample_omega_1 = TRUE,
  sample_omega_2 = TRUE,
  sample_phi_1 = TRUE,
  sample_phi_2 = TRUE,
  sample_rho = TRUE,
  sample_sigma = TRUE,
  sample_zeta_1 = TRUE,
  sample_zeta_2 = TRUE,
  sample_beta_j0_tot = TRUE,
  sample_p_beta = TRUE,
  sample_tau_i = TRUE,
  sample_theta_j = TRUE,
  sample_a1 = TRUE,
  sample_a2 = TRUE,
  sample_b1 = TRUE,
  sample_b2 = TRUE,
  sample_mu_j = TRUE,
  sample_initial_conditions = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{PATHS}{A list containing paths to various directories. If NULL, will use get_paths().}

\item{priors}{A priors list object. If NULL, will use MOSAIC::priors_default.}

\item{config}{A config template list. If NULL, will use MOSAIC::config_default.}

\item{seed}{Random seed for reproducible sampling (required).}

\item{sample_alpha_1}{Sample alpha_1 parameter (population mixing within metapops). Default TRUE.}

\item{sample_alpha_2}{Sample alpha_2 parameter (degree of frequency driven transmission). Default TRUE.}

\item{sample_decay_days_long}{Sample decay_days_long parameter (maximum V. cholerae survival). Default TRUE.}

\item{sample_decay_days_short}{Sample decay_days_short parameter (minimum V. cholerae survival). Default TRUE.}

\item{sample_decay_shape_1}{Sample decay_shape_1 parameter (first Beta shape for decay). Default TRUE.}

\item{sample_decay_shape_2}{Sample decay_shape_2 parameter (second Beta shape for decay). Default TRUE.}

\item{sample_epsilon}{Sample epsilon parameter (immunity). Default TRUE.}

\item{sample_gamma_1}{Sample gamma_1 parameter (recovery rate). Default TRUE.}

\item{sample_gamma_2}{Sample gamma_2 parameter (recovery rate). Default TRUE.}

\item{sample_iota}{Sample iota parameter (importation rate). Default TRUE.}

\item{sample_kappa}{Sample kappa parameter (spatial correlation). Default TRUE.}

\item{sample_mobility_gamma}{Sample mobility_gamma parameter. Default TRUE.}

\item{sample_mobility_omega}{Sample mobility_omega parameter. Default TRUE.}

\item{sample_omega_1}{Sample omega_1 parameter (infection). Default TRUE.}

\item{sample_omega_2}{Sample omega_2 parameter (infection). Default TRUE.}

\item{sample_phi_1}{Sample phi_1 parameter (incubation). Default TRUE.}

\item{sample_phi_2}{Sample phi_2 parameter (incubation). Default TRUE.}

\item{sample_rho}{Sample rho parameter (immunity waning). Default TRUE.}

\item{sample_sigma}{Sample sigma parameter (immunity). Default TRUE.}

\item{sample_zeta_1}{Sample zeta_1 parameter (spatial). Default TRUE.}

\item{sample_zeta_2}{Sample zeta_2 parameter (spatial). Default TRUE.}

\item{sample_beta_j0_tot}{Sample beta_j0_tot parameter (total transmission rate). Default TRUE.}

\item{sample_p_beta}{Sample p_beta parameter (proportion of human-to-human transmission). Default TRUE.}

\item{sample_tau_i}{Sample tau_i parameter (diffusion). Default TRUE.}

\item{sample_theta_j}{Sample theta_j parameter (WASH coverage). Default TRUE.}

\item{sample_a1}{Sample a1 parameter (seasonality). Default TRUE.}

\item{sample_a2}{Sample a2 parameter (seasonality). Default TRUE.}

\item{sample_b1}{Sample b1 parameter (seasonality). Default TRUE.}

\item{sample_b2}{Sample b2 parameter (seasonality). Default TRUE.}

\item{sample_mu_j}{Sample mu_j parameter (location-specific case fatality ratio). Default TRUE.}

\item{sample_initial_conditions}{Sample initial condition proportions for all compartments. Default TRUE.}

\item{verbose}{Logical indicating whether to print progress messages. Default TRUE.}
}
\value{
A MOSAIC config list with sampled parameter values.
}
\description{
This function samples parameter values from prior distributions and creates a MOSAIC config file
with the sampled values. It supports all distribution types used in MOSAIC priors and provides
explicit control over which parameters to sample.
}
\examples{
\dontrun{
# Sample all parameters (default)
config_sampled <- sample_parameters(seed = 123)

# Sample only disease progression parameters
config_sampled <- sample_parameters(
  seed = 123,
  sample_mobility_omega = FALSE,
  sample_mobility_gamma = FALSE,
  sample_kappa = FALSE
)

# Use helper for common patterns
args <- create_sampling_args("disease_only", seed = 123)
config_sampled <- do.call(sample_parameters, args)
}
}
