% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_state.R
\name{sim_check_invariants}
\alias{sim_check_invariants}
\title{Assert per-tick invariants on engine state}
\usage{
sim_check_invariants(state, tick, compartments)
}
\arguments{
\item{state}{State environment from \code{sim_alloc_state()}.}

\item{tick}{Tick index just written (1-based row into the state matrices).}

\item{compartments}{Character vector of compartments in play; \code{N} is
checked against the sum of exactly these.}
}
\value{
Invisibly \code{TRUE}; errors on violation.
}
\description{
These are checked independently of the Python oracle, which is the point:
they catch the class of bug where R and Python agree because both are
wrong, and they are the only correctness checks that survive once the
oracle is gone.
}
\keyword{internal}
