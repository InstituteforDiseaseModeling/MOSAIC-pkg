% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_rng.R
\name{sim_draws}
\alias{sim_draws}
\title{Create a draw controller for one simulation}
\usage{
sim_draws(
  mode = c("rng", "replay"),
  seed = NULL,
  record = NULL,
  tol_rel = 1e-06,
  tol_abs = 1e-09
)
}
\arguments{
\item{mode}{Either \code{"rng"} (draw from R's generator) or \code{"replay"}
(pop recorded draws from \code{record} and assert they match).}

\item{seed}{Integer seed. Used in \code{"rng"} mode.}

\item{record}{Replay record from \code{sim_read_fixture()}. Required when
\code{mode = "replay"}.}

\item{tol_rel, tol_abs}{Combined tolerance for comparing draw parameters
against the record: \code{abs(a - b) <= tol_abs + tol_rel * abs(b)}. A
purely relative tolerance is wrong here because rates legitimately reach
exactly zero in low-transmission patches.}
}
\value{
A draw controller (an environment).
}
\description{
Create a draw controller for one simulation
}
\keyword{internal}
