% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/npe.R
\name{train_npe}
\alias{train_npe}
\title{Train NPE Model}
\usage{
train_npe(
  X,
  y,
  weights = NULL,
  bounds,
  architecture,
  output_dir,
  n_epochs = 1000,
  batch_size = 512,
  learning_rate = 0.001,
  validation_split = 0.15,
  early_stopping = TRUE,
  patience = 20,
  use_gpu = FALSE,
  seed = 42,
  verbose = TRUE
)
}
\arguments{
\item{X}{Matrix of parameters (n_samples x n_params)}

\item{y}{Matrix of observations (n_samples x n_obs)}

\item{weights}{Vector of importance weights (NULL for uniform)}

\item{bounds}{Matrix of parameter bounds (n_params x 2)}

\item{architecture}{List with architecture specification}

\item{output_dir}{Directory to save trained model}

\item{n_epochs}{Maximum training epochs (default 1000)}

\item{batch_size}{Batch size for training (default 256)}

\item{learning_rate}{Initial learning rate (default 1e-3)}

\item{validation_split}{Proportion for validation (default 0.2)}

\item{early_stopping}{Use early stopping (default TRUE)}

\item{patience}{Early stopping patience (default 10, reduced to prevent overfitting)}

\item{use_gpu}{Use GPU if available (default TRUE)}

\item{seed}{Random seed}

\item{verbose}{Print progress}
}
\value{
Trained NPE model object
}
\description{
Trains a normalizing flow model for neural posterior estimation using
v5.2 advanced features including transform ramping, guards, and auto-tune.
}
