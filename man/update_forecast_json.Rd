% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manage_forecast_data.R
\name{update_forecast_json}
\alias{update_forecast_json}
\title{Update Forecast JSON Files}
\usage{
update_forecast_json(
  enso_forecasts = NULL,
  dmi_forecasts = NULL,
  last_updated = NULL,
  expires_after = NULL
)
}
\arguments{
\item{enso_forecasts}{Named list of ENSO forecasts where names are ENSO34/ENSO3/ENSO4
and values are named vectors with date keys (YYYY-MM) and numeric values}

\item{dmi_forecasts}{Named vector of DMI forecasts with date keys (YYYY-MM) and numeric values}

\item{last_updated}{Date string in YYYY-MM-DD format (default: current date)}

\item{expires_after}{Date string in YYYY-MM-DD format (default: auto-calculated)}
}
\description{
Helper function to programmatically update the forecast JSON files.
This is useful for batch updates or automated processes.
}
\examples{
\dontrun{
# Update with new forecast data
enso_data <- list(
  ENSO34 = c("2025-03" = -0.2, "2025-04" = -0.1),
  ENSO3 = c("2025-03" = -0.1, "2025-04" = 0.0),
  ENSO4 = c("2025-03" = 0.1, "2025-04" = 0.2)
)
dmi_data <- c("2025-03" = -0.1, "2025-04" = 0.0)

update_forecast_json(enso_data, dmi_data)
}

}
