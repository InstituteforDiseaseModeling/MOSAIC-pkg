% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weighted_statistics.R
\name{weighted_quantiles_presorted}
\alias{weighted_quantiles_presorted}
\title{Weighted quantiles from already-sorted, pre-filtered inputs}
\usage{
weighted_quantiles_presorted(x_sorted, w_sorted, probs)
}
\arguments{
\item{x_sorted}{Numeric vector of values, sorted ascending, all finite.}

\item{w_sorted}{Numeric vector of weights aligned with \code{x_sorted}, all
finite and positive.}

\item{probs}{Numeric vector of quantile probabilities (between 0 and 1).}
}
\value{
Vector of weighted quantiles, one per element of \code{probs}.
}
\description{
Internal core of \code{\link{weighted_quantiles}}: the cumulative-weight
interpolation, assuming inputs are already filtered (finite values, positive
weights) and sorted ascending by value with weights aligned. Exposed so hot
callers that sort once and reuse the order across many subsets (e.g.
\code{\link{optimize_ensemble_subset}}) can skip the per-call \code{order()}.
}
\seealso{
\code{\link{weighted_quantiles}}
}
