% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_json_or_gz.R
\name{write_json_or_gz}
\alias{write_json_or_gz}
\title{Write a list to .json, .json.gz, or both -- byte-equal by construction}
\usage{
write_json_or_gz(data_list, file_path, write_json = TRUE, write_gz = FALSE)
}
\arguments{
\item{data_list}{Named list to serialise.}

\item{file_path}{Base output path. Must end in \code{.json}. The \code{.gz}
output (if \code{write_gz = TRUE}) is written to
\code{paste0(file_path, ".gz")}.}

\item{write_json}{Logical. If \code{TRUE} (default), write the plain
\code{.json} file.}

\item{write_gz}{Logical. If \code{TRUE}, write the gzipped \code{.json.gz}
file. Default \code{FALSE}.}
}
\value{
The base \code{file_path} (invisibly).
}
\description{
Serialises \code{data_list} to JSON text once and writes that single
in-memory string to either the plain \code{.json} file, the gzipped
\code{.json.gz} file, or both -- as selected by the \code{write_json} and
\code{write_gz} arguments. Because both outputs are written from the same
byte string, the \code{.json.gz} always decompresses to a file byte-
identical to the \code{.json} (when both are produced); the pair cannot
drift.

Replaces the older \code{write_json_with_optional_gz()} "sidecar" model.
Both flags are first-class peers: any combination except
\verb{write_json = FALSE, write_gz = FALSE} is allowed.
}
\details{
Both files come from a single \code{jsonlite::toJSON()} call. There is no
parallel serialisation, no patch-and-rewrite loop, and no possibility
for the \code{.json} and \code{.json.gz} to diverge.

At least one of \code{write_json} / \code{write_gz} must be \code{TRUE}; passing
both as \code{FALSE} errors immediately (the function would otherwise
produce nothing).
}
\examples{
\dontrun{
  # Plain JSON only (default)
  write_json_or_gz(my_config, "config.json")

  # gzip only
  write_json_or_gz(my_config, "config.json", write_json = FALSE, write_gz = TRUE)

  # Both, guaranteed byte-equal
  write_json_or_gz(my_config, "config.json", write_json = TRUE, write_gz = TRUE)
}

}
\seealso{
\code{\link[=write_list_to_json]{write_list_to_json()}}
}
