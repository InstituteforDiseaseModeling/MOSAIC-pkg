% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_json_with_optional_gz.R
\name{write_json_with_optional_gz}
\alias{write_json_with_optional_gz}
\title{Write a list to a .json file and optionally produce a byte-equal .json.gz sidecar}
\usage{
write_json_with_optional_gz(data_list, file_path, gz_sidecar = FALSE)
}
\arguments{
\item{data_list}{Named list to serialise.}

\item{file_path}{Output path. Must end in \code{.json} (the .gz sidecar
is derived from this path).}

\item{gz_sidecar}{Logical. If \code{TRUE}, also write \verb{<file_path>.gz} as a
gzip of the .json. Default \code{FALSE}.}
}
\value{
The path to the .json file (invisibly).
}
\description{
Writes \code{data_list} to \code{file_path} using \code{\link[=write_list_to_json]{write_list_to_json()}} (plain
.json, no compression). If \code{gz_sidecar = TRUE}, additionally produces
\verb{<file_path>.gz} by running \code{\link[R.utils:compressFile]{R.utils::gzip()}} on the just-written file.

Because the .gz is derived from the exact bytes on disk, it is
guaranteed to decompress to a byte-identical copy of the .json -- there
is no parallel \code{jsonlite::toJSON()} call and therefore no opportunity
for the two outputs to drift.
}
\details{
Intended for the data-raw config builders that ship LASER configuration
artifacts under \verb{inst/extdata/}. Production callers can flip the
sidecar on via the \code{MOSAIC_WRITE_GZ_SIDECARS} environment variable
(see \code{\link[=.mosaic_write_gz_sidecars]{.mosaic_write_gz_sidecars()}}) without editing source.
}
\seealso{
\code{\link[=write_list_to_json]{write_list_to_json()}}, \code{\link[R.utils:compressFile]{R.utils::gzip()}},
\code{\link[=.mosaic_write_gz_sidecars]{.mosaic_write_gz_sidecars()}}
}
