# Draw Posterior Samples (Helper for Rejection Sampling)

Draw Posterior Samples (Helper for Rejection Sampling)

## Usage

``` r
.draw_posterior_samples(
  npe_model,
  obs_norm,
  n,
  normalization,
  device,
  return_log_probs,
  torch
)
```
