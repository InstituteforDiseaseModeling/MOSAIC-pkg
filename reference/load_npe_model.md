# Load NPE Model

Loads a saved NPE model from disk.

## Usage

``` r
load_npe_model(model_dir, device = "cpu")
```

## Arguments

- model_dir:

  Directory containing saved model

- device:

  Device to load model to ("cpu" or "cuda")

## Value

NPE model object
